# Database (Neon)
DATABASE_URL=""

# NextAuth (set strong secret in production; Vercel: 32+ random bytes)
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Public site URL for canonicals, OG URLs, sitemap
NEXT_PUBLIC_SITE_URL="https://masrjobs.org"

# Only "true" for local UX demos — never enable on production
NEXT_PUBLIC_ENABLE_DEMO_AUTH="false"

# Optional: forgot-password emails (https://resend.com)
# RESEND_API_KEY=""
# EMAIL_FROM="MasrJobs <onboarding@resend.dev>"
